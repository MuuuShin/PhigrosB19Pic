import sqlite3
import pyDes

# Complete the following code
# The database need have 'songs2nex' table including 'id', 'sname', 'difficultyEZ', 'difficultyHD', 'difficultyIN', 'difficultyAT' columns.
DES_SECRET_KEY = ''
conn = sqlite3.connect('')
